= Install the Red Hat Build of Kueue
:navtitle: Install Kueue

In this chapter, you install the Red Hat build of Kueue as a standalone operator and configure it for GPU quota management.

The Helm chart installs *only* the operator -- all Kueue resources (ResourceFlavors, ClusterQueues, LocalQueues, WorkloadPriorityClasses) are created manually in the xref:kueue-config:index.adoc[next chapter] so you understand how each piece fits together.

== Why Kueue Is Installed Separately from RHOAI

RHOAI includes a bundled Kueue component (via the DataScienceCluster), but the RHOAI Helm chart sets it to `Unmanaged` so RHOAI does not interfere with the standalone operator.
Installing Kueue independently lets you:

* Pin a specific Kueue version
* Configure the operator separately from the RHOAI lifecycle
* Understand how Kueue works as a standalone Kubernetes component

IMPORTANT: The embedded version of Kueue in RHOAI is deprecated. The only supported method is to install the Red Hat build of Kueue operator separately.

== What You Will Do

. Install the Kueue operator via Helm
. Wait for the operator to reach `Succeeded`
. Patch the Kueue CR to disable LocalQueue defaulting and enable `BatchJob` integration
. Verify the Kueue CRDs are registered on the cluster

=== References

* https://kueue.sigs.k8s.io/docs/overview/[Kueue: Overview^]
* https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.4/html/managing_openshift_ai/managing-workloads-with-kueue[Red Hat OpenShift AI: Managing Workloads with Kueue^]
* https://docs.redhat.com/en/documentation/openshift_container_platform/4.20/html-single/ai_workloads/index#about-kueue[Red Hat build of Kueue: Overview^]
* https://github.com/kubernetes-sigs/kueue[Kueue — GitHub (upstream)^]

.Kueue vs Kubernetes Scheduler
[sidebar]
****
Kueue does not replace the Kubernetes scheduler.
Instead, it acts as an *admission controller* that decides whether a workload should be allowed to run based on quota, priority, and fair sharing rules.
Once Kueue admits a workload, the Kubernetes scheduler handles the actual pod-to-node placement using nodeSelectors, tolerations, and resource requests.

Think of Kueue as the bouncer at the door (who gets in?) and the Kubernetes scheduler as the host inside (where do they sit?).
****
