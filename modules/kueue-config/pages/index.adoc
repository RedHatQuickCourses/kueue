= Configure Kueue Resources
:navtitle: Configure Kueue

With the Kueue operator running, you now create the Kueue resources manually to learn how GPU quota management works.
All YAML files are in the `kueue-config/` directory and are applied in order.

== What You Will Create

You create five sets of resources, each building on the previous one:

. *Namespaces* -- Kueue-managed namespaces for training and inference workloads
. *ResourceFlavors* -- map Kueue quotas to specific GPU hardware on specific nodes
. *ClusterQueues* -- define cluster-wide GPU, CPU, and memory quotas per GPU type
. *LocalQueues* -- expose ClusterQueue capacity to specific namespaces
. *WorkloadPriorityClasses* -- control queueing order and preemption within Kueue

== Architecture Overview

The following diagram shows how the Kueue resources connect to form a complete GPU quota management system.
Workloads enter through a LocalQueue, are forwarded to a ClusterQueue for admission and quota checks, and when admitted, the ResourceFlavor injects the correct `nodeSelector` and `tolerations` to place pods on the right GPU nodes.

image::kueue-resource-hierarchy.svg[Kueue Resource Hierarchy,width=1000]

.How the Pieces Fit Together
[sidebar]
****
Think of Kueue's resource hierarchy as a supply chain:

* *ResourceFlavors* describe _what hardware exists_ (A100s on worker-1, T4s on worker-2)
* *ClusterQueues* set _how much_ of that hardware can be consumed (quotas) and _who gets priority_ (preemption)
* *LocalQueues* control _who can access_ the quota (namespace-scoped entry points)
* *WorkloadPriorityClasses* determine _who goes first_ when the queue is full

A workload enters through a LocalQueue, is forwarded to the backing ClusterQueue for admission, and if admitted, gets scheduled on nodes matching the ResourceFlavor's labels.
****

=== References

* https://kueue.sigs.k8s.io/docs/concepts/[Kueue: Core Concepts^]
* https://kueue.sigs.k8s.io/docs/concepts/resource_flavor/[Kueue: ResourceFlavor^]
* https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/[Kueue: ClusterQueue^]
* https://kueue.sigs.k8s.io/docs/concepts/local_queue/[Kueue: LocalQueue^]
* https://kueue.sigs.k8s.io/docs/concepts/workload_priority_class/[Kueue: WorkloadPriorityClass^]
* https://docs.redhat.com/en/documentation/openshift_container_platform/4.20/html-single/ai_workloads/index#configuring-quotas[Red Hat build of Kueue: Configuring Quotas^]

