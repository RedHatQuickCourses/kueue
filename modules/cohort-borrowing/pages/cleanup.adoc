= Clean Up and Revert
:navtitle: Clean Up

Clean up all cohort workloads and revert to the original isolated queue configuration.

== References

* https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/#cohort[Kueue: Cohort^]
* https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/#fair-sharing[Kueue: Fair Sharing (preview of next chapter)^]
* https://docs.redhat.com/en/documentation/openshift_container_platform/4.20/html-single/ai_workloads/index#using-cohorts[Red Hat build of Kueue: Using Cohorts^]
* https://docs.redhat.com/en/documentation/openshift_container_platform/4.20/html-single/ai_workloads/index#configuring-fairsharing[Red Hat build of Kueue: Configuring Fair Sharing^]

== Lab: Clean Up

=== 1. Clean up all cohort workloads

a. Delete training jobs:
+
[source,console,subs="+quotes"]
----
$ **oc delete -f workloads/job-reclaim-preemption.yaml**
job.batch "training-reclaim-1" deleted from training-jobs namespace
job.batch "training-reclaim-2" deleted from training-jobs namespace
job.batch "training-reclaim-3" deleted from training-jobs namespace
job.batch "training-reclaim-4" deleted from training-jobs namespace
----

b. Delete inference deployments:
+
[source,console,subs="+quotes"]
----
$ **oc delete deployment inference-fill-1 inference-fill-2 inference-borrow -n inference-apps**
deployment.apps "inference-fill-1" deleted from inference-apps namespace
deployment.apps "inference-fill-2" deleted from inference-apps namespace
deployment.apps "inference-borrow" deleted from inference-apps namespace
----

c. Verify everything is cleaned up:
+
[source,console,subs="+quotes"]
----
$ **oc get pods -n training-jobs**
No resources found in training-jobs namespace.

$ **oc get pods -n inference-apps**
No resources found in inference-apps namespace.
----

=== 2. Revert to isolated queues (undo cohort)

a. Remove cohort borrowing and return to the original isolated queue configuration:
+
[source,console,subs="+quotes"]
----
$ **oc apply -f kueue-config/3-cluster-queues.yaml**
clusterqueue.kueue.x-k8s.io/a100-cluster-queue configured
clusterqueue.kueue.x-k8s.io/t4-cluster-queue configured
----
+
This overwrites both ClusterQueues with the original definitions -- no cohort, no borrowing, no lending.

== Summary

You have demonstrated the full cohort borrowing lifecycle:

. *Cohort setup* -- joined both ClusterQueues into `gpu-cohort` with lending and borrowing limits
. *Borrowing* -- the T4 pool exhausted its 2 GPUs, and a third workload borrowed 1 GPU from the A100 pool. The borrowed pod landed on the A100 node (visible in `nvidia-smi` output)
. *Reclaim failure* -- without `reclaimWithinCohort`, the A100 pool could not take back its lent GPU even when its own workloads needed it. The 4th training job stayed pending
. *Reclaim fix* -- adding `reclaimWithinCohort: Any` let the A100 pool evict the borrowed workload and admit its own 4th training job

== Cohort Configuration Summary

[cols="2,3,1", options="header"]
|===
|File |Resources |Scope

|`9-cohort-cluster-queues.yaml`
|Cohort ClusterQueues without reclaim (borrowing only)
|Cluster

|`9a-cohort-reclaim-queues.yaml`
|Cohort ClusterQueues with `reclaimWithinCohort: Any` (reclaim fix)
|Cluster

|`deployment-cohort-borrowing.yaml`
|3 inference Deployments (2 fill T4, 1 borrows from A100)
|Namespace

|`job-cohort-nopreempt.yaml`
|4 training Jobs to demonstrate reclaim failure
|Namespace

|`job-reclaim-preemption.yaml`
|4 training Jobs to demonstrate reclaim success
|Namespace
|===

In the xref:fair-sharing:index.adoc[next chapter], you learn how Kueue uses Dominant Resource Share (DRS) to make *proportional* preemption decisions across teams.
In this chapter, reclaim was all-or-nothing -- either the lender can reclaim or it cannot.
Fair sharing goes further by considering *how much* each team is consuming relative to its fair share before deciding whether to preempt.

TIP: For a complete cluster reset (removing ALL lab resources), see xref:cluster-reset:index.adoc[Cluster Reset].
